node_modules/
dist/
src/server/public/

# `npm pack` writes the tarball into the directory it is run from, and the
# acceptance check for a release runs it here.
*.tgz
*.db
*.db-wal
*.db-shm
.env
.DS_Store
coverage/

# Raw stage-0 OTLP captures: contain the capturing account's real email and
# account identifiers. Sanitised copies live in test/fixtures/.
captures/
